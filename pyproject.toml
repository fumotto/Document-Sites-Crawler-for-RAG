[project]
name = "document_sites_crawler_for_rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "httpx>=0.28.1",
    "pytest-cov>=7.1.0",
    "python-dotenv>=1.0,<2",
    "tiktoken>=0.7,<1",
    "trafilatura>=1.8,<2",
]

[dependency-groups]
dev = [
    "pytest>=7.0,<9",
    "playwright>=1.40.0,<2",
    "pytest-playwright>=0.5.0,<1",
    "pywebview>=5.0,<6",
    "pywin32>=306 ; sys_platform == 'win32'",
]
