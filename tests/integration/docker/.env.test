MAX_PAGES=10
TIMEOUT_SECONDS=5
# 以下2つはホスト側（pytest）がtestserverの管理エンドポイントへ到達する際の
# 値。crawlerコンテナ自身はこの値を使わない（クロール対象URLはCLI引数
# CRAWLER_TARGET_URL = http://testserver:8080 で別途渡される）。
TESTSERVER_HOST=localhost
TESTSERVER_PORT=8080
LOG_LEVEL=INFO